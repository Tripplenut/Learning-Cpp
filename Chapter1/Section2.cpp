#include <iostream> //Imports the Inputs and Outputs
int main(){
    std::cout<<"Enter two numbers:" << std::endl; //Output and creates a new line
    int v1, v2; //Initialized Integers
    std::cin>>v1>>v2;
    std::cout<<"The sum of "<<v1<<" and "<<v2<<" is "<<v1+v2<<std::endl;
    return 0;
}